<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>pers.zkx</groupId>
        <artifactId>algo-java</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>algo-api</artifactId>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <thrift.version>0.14.2</thrift.version>
        <maven.thrift.plugin.version>0.1.11</maven.thrift.plugin.version>
    </properties>

    <dependencies>
        <!-- Thrift runtime -->
        <dependency>
            <groupId>org.apache.thrift</groupId>
            <artifactId>libthrift</artifactId>
            <version>${thrift.version}</version>
        </dependency>

        <!-- Add test deps if needed -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>


    <build>
        <plugins>
            <!-- Thrift Maven Plugin: generate Java sources from .thrift -->
            <plugin>
                <groupId>org.apache.thrift.tools</groupId>
                <artifactId>maven-thrift-plugin</artifactId>
                <version>${maven.thrift.plugin.version}</version>
                <executions>
                    <execution>
                        <id>thrift-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <generator>java</generator>
                    <thriftExecutable>D:\Environment\thrift\thrift.exe</thriftExecutable>
                    <!-- 可选: 如果 CI/开发机没有 thrift 可执行文件, 可以在本地先用 thrift CLI 生成源到 src/main/java -->
                    <!-- <thriftExecutable>/usr/local/bin/thrift</thriftExecutable> -->
                    <!-- 如果想把生成的代码放入特定目录，可通过 outputDirectory 指定 -->
                    <!-- <outputDirectory>${project.build.directory}/generated-sources/thrift</outputDirectory> -->
                </configuration>
            </plugin>

            <!-- Standard compiler plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
